# Symfony + PHP 7.4 + redis

```bash
docker-compose build app
docker-compose up -d
```

### configuration ( `.env` )
`HOST_APP_ROOT`  : set your symfony project path, for exammple: `../my_project`

### access
http://localhost:8888

